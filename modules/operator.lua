-- TODO: Do I ever want to do something with this?

G.FUNCS.calculate_round_score = function (hand_chips, mult)
    return hand_chips * mult
end

G.FUNCS.get_operation_title = function(e)
    G.operation_title = "X"
end
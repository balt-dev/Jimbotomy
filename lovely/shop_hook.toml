[manifest]
version = "1.0.0"
priority = -1000

[[patches]]
[patches.pattern]
target = "game.lua"
pattern = "local shop_exists = not not G.shop"
position = "after"
payload = '''if not shop_exists then jimbotomy_on_enter_shop_hook() end'''
match_indent = true
times = 1

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if (((e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0)) or (not e.config.ref_table:can_use_consumeable())) then"
position = "at"
payload = '''if (((e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0)) or (not e.config.ref_table:can_use_consumeable())) or G.GAME.modifiers.shop_disabled then'''
match_indent = true
times = 1
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if (e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0) then"
position = "at"
payload = '''if (e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0) or G.GAME.modifiers.shop_disabled then'''
match_indent = true
times = 1
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if (((e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0)) or (not e.config.ref_table:can_use_consumeable())) then"
position = "at"
payload = "if (((e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) and (e.config.ref_table.cost > 0)) or (not e.config.ref_table:can_use_consumeable())) or G.GAME.modifiers.shop_disabled then"
match_indent = true
times = 1
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at then"
position = "at"
payload = "if (e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) or G.GAME.modifiers.shop_disabled then"
match_indent = true
times = 1
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if (e.config.ref_table.cost) > 0 and (e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) then"
position = "at"
payload = "if (e.config.ref_table.cost) > 0 and (e.config.ref_table.cost > G.GAME.dollars - G.GAME.bankrupt_at) or G.GAME.modifiers.shop_disabled then"
match_indent = true
times = 1
overwrite = true

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if ((G.GAME.dollars-G.GAME.bankrupt_at) - G.GAME.current_round.reroll_cost < 0) and G.GAME.current_round.reroll_cost ~= 0 then"
position = "before"
payload = "local can_reroll = true"
match_indent = true
times = 1

[[patches]]
[patches.pattern]
target = "functions/button_callbacks.lua"
pattern = "if ((G.GAME.dollars-G.GAME.bankrupt_at) - G.GAME.current_round.reroll_cost < 0) and G.GAME.current_round.reroll_cost ~= 0 then"
position = "after"
payload = '''can_reroll = false
elseif G.GAME.modifiers.jimbotomy_criminal then
can_reroll = false
end
if not can_reroll then'''
match_indent = true
times = 1
